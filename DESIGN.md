# Design Language — 학교 우산 대여 시스템

> **베이스:** 친근한 Material 계열 시스템 (구글 블루, 부드러운 그림자, 둥근 카드)
> **영감:** [Streamtime](https://www.streamtime.net) — 단, *전체 미감*이 아니라 **타이포그래피 리듬과 숫자 강조**에만 영향을 받는다. 굵은 히어로 숫자, 섹션 간 강한 리듬감은 가져오되, 브루탈리즘적 고대비/검은 본문은 따르지 않는다.
>
> 두 방향이 충돌할 때는 **Material 베이스가 이긴다.** Streamtime은 "어디를 굵게/크게 강조할지"를 정할 때만 참고한다.

---

## 1. 디자인 원칙

### 1-1. 명확함이 친절함이다
학생은 빠르게, 교사는 정확하게. 불필요한 요소를 덜어내고 핵심 동작 하나를 크게 강조한다.

### 1-2. 상태가 곧 메시지다
색상, 뱃지, 테두리는 장식이 아니라 정보다. 빌릴 수 있다 / 빌려졌다 / 연체다 — 이 세 가지 상태는 어디서 봐도 즉시 읽혀야 한다.

### 1-3. 모바일 퍼스트, 터치 퍼스트
QR로 진입하는 학생은 스마트폰을 한 손에 들고 있다.
- 탭 타겟 **최소 48px** (현재 `.tab` 높이가 ~46px이므로 `min-height:48px`로 맞춘다).
- 핵심 동작 버튼은 `btn-block`으로 **폭을 꽉 채워** 큰 터치 타겟을 만든다.
- 스캔 → 결과 → 대여 버튼이 **스크롤 없이** 닿도록 한다.
- 탭 바는 상단 sticky로 항상 접근 가능하게 둔다. (하단 탭바는 §7 개선 후보)

### 1-4. 학생 / 관리자 경험을 분리하라
학생 화면(스캔 탭)은 최대한 단순하게. 관리자 도구는 로그인 뒤에만 드러난다. 두 경험이 섞이면 둘 다 나빠진다.

---

## 2. 톤 & 무드

| 항목 | 방향 |
|---|---|
| **감성** | 학교 현장 — 정돈되어 있지만 딱딱하지 않음 |
| **언어** | 반말 아닌 짧은 존댓말. "확인해요", "반납할게요" 수준 |
| **밀도** | 여백을 넉넉히. 콘텐츠가 숨쉬어야 한다 |
| **장식** | 최소한. 이모지는 아이콘 대용으로만 (☂️ ✓) |
| **모션** | 빠르게 (150-250ms). 화면 전환 fade+slide-up 유지 |

---

## 3. 색상 시스템

### 브랜드 컬러
```
--primary:      #1a73e8   /* 구글 블루 계열 — 신뢰, 링크, CTA */
--primary-dark: #1557b0   /* 호버/포커스 */
```

### 상태 컬러 (변경 금지 — 정보 전달 역할)
```
--avail:    #16a34a  bg: #dcfce7   /* 대여 가능 — 초록 */
--rented:   #ea8600  bg: #fef3c7   /* 대여 중 — 주황 */
--overdue:  #dc2626  bg: #fee2e2   /* 연체 — 빨강 */
```

### 중립 팔레트
```
--bg:     #f1f5f9   /* 페이지 배경 — 연한 슬레이트 */
--card:   #ffffff   /* 카드 배경 */
--text:   #1f2937   /* 본문 */
--muted:  #6b7280   /* 보조 텍스트 */
--border: #e5e7eb   /* 구분선 */
```

### 다크 강조 (Streamtime에서 영감)
헤더는 `linear-gradient(135deg, #1a73e8, #4f46e5)` — 단색 대신 그라디언트로 무게감을 준다.
섹션 분리가 필요한 경우 `#1f2937` (다크 슬레이트) 배경 + 흰 텍스트 조합을 활용한다.

---

## 4. 타이포그래피

```
font-family: -apple-system, "Malgun Gothic", "Apple SD Gothic Neo", sans-serif
```

| 역할 | 크기 | 굵기 | 용례 |
|---|---|---|---|
| 히어로 숫자 | 26-38px | 800 | 우산 번호, 통계 숫자 |
| 섹션 제목 | 17-21px | 700 | 카드 타이틀, 헤더 h1 |
| 본문 | 14-16px | 400 | 설명, 폼 레이블 |
| 보조 | 12-13px | 400-600 | 뮤트 텍스트, 태그 |

**원칙:** 숫자는 크고 굵게. 상태 정보(우산 번호, 대여 인원 수)는 한눈에 들어와야 한다.

---

## 5. 스페이싱 & 레이아웃

```
max-width: 760px   /* 모바일 우선, 태블릿까지 커버 */
--radius: 14px     /* 카드 모서리 */
card padding: 18px
content padding: 16px
gap between cards: 16px
```

**섹션 리듬 (Streamtime에서 영감)**
- 각 카드는 한 가지 역할만 한다 (스캔, 등록, 통계 혼재 금지)
- 통계 그리드 → 리스트 순서로 읽힌다: 숫자(요약) → 상세
- 버튼과 입력 최소 높이: 48px (터치 타겟)

---

## 6. 컴포넌트 패턴

### 카드
```
background: white
border-radius: 14px
box-shadow: 0 1px 3px rgba(0,0,0,.08), 0 4px 12px rgba(0,0,0,.05)
padding: 18px
```

### 상태 카드 (result-card)
왼쪽 6px 컬러 보더로 상태를 즉시 전달. 배경은 흰색 유지 (색 범람 금지).

### 버튼 계층
1. `btn-primary` (파랑) — 주요 동작 하나만
2. `btn-success` (초록) — 대여 확정
3. `btn-danger` (빨강) — 강제 반납
4. `btn-outline` — 보조 동작
5. `btn-text` — 로그아웃, 새로고침, **관리자 진입 링크** 등 최소 노출 동작

### 뱃지 & 태그
둥근 pill 형태. 텍스트+배경 모두 상태 컬러 사용. 절대 상태 컬러를 임의로 섞지 않는다.

### 토스트
화면 하단 중앙 고정. ok(초록) / err(빨강) / 중립(다크) 세 가지만.

---

## 7. 개선 방향 (Streamtime 영감 적용)

"현재" 칸은 **실제 구현 상태**를 정확히 기준으로 한다.

| 현재 (실제) | 개선 방향 | 우선순위 |
|---|---|---|
| 학생도 탭 4개가 모두 보임 (탭이 곧 관리자 진입점) | 학생 모드에선 **스캔 탭만 노출**, 나머지 3개는 로그인 후 표시. 진입점은 **푸터의 작은 "관리자" 링크**(`btn-text`)로 분리 — §1-4 실현 | **상** |
| 헤더가 모든 탭에서 동일하게 보임 | 스캔 탭에서 헤더를 **히어로 영역**으로 확대, 우산 번호 입력을 중앙에 크게 (단, 헤더 2개 모드가 생기는 복잡도는 감수) | 중 |
| 상태 조회 후 대여 폼이 *즉시* 렌더됨 (이미 단계적 노출) | 폼 등장에 **slide-down 애니메이션**(≤250ms) 추가 — 동작은 그대로, 전환만 부드럽게 | 중 |
| 통계 숫자는 **이미 색상 구분됨** (연체=빨강 등), 4개 크기만 동일 | 연체 숫자만 **더 크게** 강조 (색은 이미 done) | 하 |
| 빈 상태 메시지가 텍스트뿐 | **아이콘 + 메시지** 조합 empty state 컴포넌트화 | 하 |
| 핵심 동작이 상단/카드 내부에 분산 | 하단 탭바(bottom nav) 도입 검토 — §1-3 "엄지 영역" 원칙의 실제 구현 | 검토 |

> **이미 반영된 것 (다시 만들지 말 것):** 통계 숫자 색상 구분(`.stat-*-num`), 스캔 후 상태별 폼 분기(`renderScanResult`), 상태 컬러 보더 카드(`result-card`).

---

## 8. 하지 말 것

- 상태 컬러 외 임의 색상 추가 — 팔레트를 더 이상 늘리지 않는다
- 학생 화면에 관리 기능 노출 (관리자 진입은 푸터 링크 한 곳으로만)
- 카드 안에 카드 중첩
- 애니메이션 duration 250ms 초과 (느리게 느껴짐 — §2 모션 기준과 일치)
- 모바일에서 수평 스크롤이 생기는 레이아웃 (단, `대여 기록` 테이블은 `.table-wrap` 내부 스크롤만 허용)
- Streamtime을 이유로 본문을 새카맣게/고대비로 바꾸기 — 베이스는 Material이다 (서두 참조)
